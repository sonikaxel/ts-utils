# Utility function for typescript project.

This project contains utility functions required for day-to-day life in coding 😂.

All the utility functions can be imported from as follow:

```ts
import * as utils from '~~/utils';
```

> **Note:** `~~/` means root directory of project.

Utils function which used external libraries are in seperate folders of utils folder which can be found under directory `~~/utils`.

Kindly delete those utils if you don't use such external libraries in your project.
